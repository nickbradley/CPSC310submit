version: '2'
services:
  redis:
    image: redis:3.2.1-alpine
  db:
    image: couchdb:1.6.1
    expose: "5984"
  web:
    build: ./pull-request
    ports:
      - "4430"
    links:
      - redis
  nginx:
    build: ./nginx
    ports:
      - "443:443"
    links:
      - web
      - db
